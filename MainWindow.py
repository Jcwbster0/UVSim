import tkinter as tk
from tkinter import ttk

class MainWindow(tk.Tk()):
    
    def __init__(self): 
       super().__init__(self, title, size, elements) 
       self.title(title)
       self.geometry(f"{size[0]}x{size[1]}")
       self.minsize(size[0], size[1])
       self.title = title
       
       #initialize ui widgets here
        

       
       
       self.mainloop()
        
